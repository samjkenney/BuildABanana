// import { Scene } from "phaser";
// import { GameObjects } from "phaser";
// import { Button } from './Button';

// export class TextButton extends Button{
//     constructor(scene: Scene, x: number, y: number, width: number, height: number, color: number, displayText: string, textStyle: GameObjects.Text, action: Function){
//         super(scene, x, y, width, height, color, action);

//         var text = new GameObjects.Text(scene, width / 2, height / 2, displayText, textStyle); //figure out text styling, CSS
//         this.scaleToButton(text);

//         this.add(text);
//     }

//     //click???
// }